{
  "scripts": {
    "dev": "json-server --watch filejsonAPI/db5.json --port 3000",
    "build": "echo 'Build script not needed for json-server'"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
